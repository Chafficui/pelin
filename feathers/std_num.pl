fn num add(num a, num b) {
    RUST[std_func::add](a, b)
}

fn num subtract(num a, num b) {
    RUST[std_func::subtract](a, b)
}

fn num multiply(num a, num b) {
    RUST[std_func::multiply](a, b)
}

fn num divide(num a, num b) {
    RUST[std_func::divide](a, b)
}

fn num sqrt(num a) {
    RUST[std_func::sqrt](a)
}